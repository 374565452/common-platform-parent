@2017-4-1 8:40
	在测试时，一直不能从数据库获取数据，原因在于数据库名称写错拉，正确的为common_platform。但有一个问题数据库写错，为什么没有报错，猜测原因，是由于druid连接池做的优化
	
@2017-4-5 17:00
	在测试时，由于将input type="hidden" 类型的文本框中的数据清空拉，实际没有传送数据，后台利用Model来接收数据，导致访问不成功。但是后台一样没有报错，只是前端没有任务响应
	不知道是springMVC model接收参数的限制，还是前端hidden文本框的问题（但在浏览器中通过调试模式显示为的，服务器不响应）
	@2017-4-10 10：10 解决，原因在于 springMVC框架中，对于传入参数的限制，如果使用annotation来动态获取参数，如果参数不存在，则会出现500错误
	
@2017-4-7 8：57
	菜单是动态生成的，页面初始化从数据库中获取数据，然后显示在页面上。但click事件无法点击。
	jquery中必须以on的方式来实现事件的绑定工作，具体可参照side_menu.js中实现的
	
@2017-4-11 11:20 
	在sql语句的结束处不能有;因为mybatis在进行分页查询时，会在sql语句的末端加入limit语法，如果有;则会出现语法错误
